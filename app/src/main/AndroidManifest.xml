<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Analyse duurt minuten en mag niet stoppen als het scherm uitgaat. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Transcriptie en segmentatie lopen via externe diensten. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Alleen video's die de gebruiker zelf kiest; geen brede opslagtoegang. -->
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <application
        android:label="Video-editor"
        android:supportsRtl="true"
        android:theme="@style/Theme.VideoEditor">

        <activity
            android:name=".EditorActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".analysis.AnalysisService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

    </application>
</manifest>
